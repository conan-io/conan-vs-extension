<UserControl x:Class="conan_vs_extension.ConanConfiguration"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:conan_vs_extension"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Orientation="Vertical" Margin="10">
            <TextBox x:Name="PathTextBox" Margin="0,0,0,10"/>
            <Button Content="Browse..." Click="BrowseButton_Click" Margin="0,0,0,10"/>
            <CheckBox x:Name="UseSystemConanCheckBox" Content="Use Conan installed in the system" Checked="UseSystemConanCheckBox_Checked" Unchecked="UseSystemConanCheckBox_Unchecked"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                <Button Content="OK" Click="OkButton_Click" Margin="0,0,10,0"/>
                <Button Content="Cancel" Click="CancelButton_Click"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
